## HW1: Микросервисная архитектура (ML System)

Проект представляет собой имитацию ML-системы в продакшене. Реализован обмен сообщениями между сервисами через RabbitMQ, сбор метрик и визуализация ошибок в реальном времени.
Для реализации архитектурного паттерна микросервисов я выбрала датасет Diabetes, потому что он был теоретической части курса, ну и сам по себе удобен, так как встроен в sklearn, что упрощает сборку контейнера и делает код переносимым без внешних зависимостей (файлов CSV)

### 🛠 Технологии
* **Python 3.9**
* **RabbitMQ** (брокер сообщений)
* **Docker & Docker Compose** (контейнеризация)
* **Pika** (клиент AMQP)
* **Matplotlib/Pandas** (визуализация)

### 🏗 Архитектура
Система состоит из 5 контейнеров:

1. **rabbitmq** - Брокер сообщений.
2. **features** - Симулирует поток данных. Берет случайную запись из датасета (Diabetes dataset), генерирует уникальный ID и отправляет:
    * Вектор признаков -> в очередь `features`.
    * Истинное значение -> в очередь `y_true`.
3. **model** - Слушает очередь `features`, делает предсказание (используя LinearRegression) и отправляет результат в очередь `y_pred`.
4. **metric** - Слушает очереди `y_true` и `y_pred`. Объединяет записи по ID, вычисляет абсолютную ошибку (Absolute Error) и логирует результат в `logs/metric_log.csv`.
5. **plot** - В бесконечном цикле читает лог-файл и обновляет гистограмму распределения ошибок `logs/error_distribution.png`.

### 🚀 Как запустить

1. Склонируйте репозиторий:
```bash
git clone [https://github.com/mrskbj/ML-System.git](https://github.com/mrskbj/ML-System.git)
```

2. Перейдите в папку проекта:
```bash
cd ML-System
```

3. Запустите docker-compose:
```bash
docker-compose up --build
```
### 📊 Результат работы
После запуска в папке `./logs` на хост-машине появятся:  
 * `metric_log.csv` - таблица с метриками.
 * `error_distribution.png` - график распределения ошибок, обновляемый в реальном времени.

Чтобы остановить сервисы, нажмите `Ctrl+C`.

### 👀 Пример результатов 

